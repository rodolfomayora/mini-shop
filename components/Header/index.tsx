import React, { FC } from 'react';

const Header: FC = () => {
  return (
    <header style={{backgroundColor: 'hsl(0, 0%, 60%)'}}>
      <h1>HEADER</h1>
      <span>Cart</span>
    </header>
  )
}

export default Header;